#go@ plink m1 -t ~/sdk/tests/test-all
#!/bin/bash
cd "${0%test-all}" || exit 1

# for f in *_test.lua; do
# 	echo "$f"
# 	AUTO=1 ../bin/luajit -e"require'glue';luapath'.'" "$f" >/dev/null
# done

for f in ../lua/*.lua; do
	echo "$f"
	AUTO=1 ../bin/luajit -e"package.path=package.path..';./?.lua'" "$f" >/dev/null || break
done
